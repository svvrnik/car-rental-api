#Spring configuration
spring.application.name=car-rental-api
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

#Postgres configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/car_rental_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:testPassword}
spring.datasource.driver-class-name=org.postgresql.Driver

#Variables
app.company.email=jwarys.carrentalapi@gmail.com

#JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

#JWT configuration
#secret only for educational purposes - not using on production
jwt.secret=${JWT_SECRET:jkrzERduXiDX3CzfQL08FdL1oAr4PbwBMy6GrQAY4VG} 
jwt.expiration =86400000

#MinIO configuration
minio.url=${MINIO_URL:http://localhost:9000}
minio.access.key=${MINIO_ACCESS_KEY:admin123}
minio.secret.key=${MINIO_SECRET_KEY:password123}
minio.bucket.name=${MINIO_BUCKET:car-images}